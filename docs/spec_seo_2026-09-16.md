# めぐり自転車 SEO・計測 改善 要件定義（2026-09-16）

（オーナーから 2026-09-16 に受領した原文。T0〜T4 の作業はこの文書を正とする。進捗は末尾「進捗」に追記）

## 0. 前提・不変条件（CLAUDE.md が最優先。矛盾があれば CLAUDE.md に従い、矛盾点を報告）
- 禁止語：「高価買取」「転売」「国家資格」。転売の意味が必要な箇所は「リユース品価格調査」。
- 電話CTAなし（LINEのみ）。実名・郵便番号・住所を推測させる記述を公開ページに載せない。転売・利益を匂わせる表現なし。
- 保護ファイル（変更禁止）：/kaitori/ PWA、LINEプリセット文の末尾サフィックス、js/tailwind-config.js、robots.txt、sitemap.xml。
- 本番＝master。本文・見出し・meta の変更は「変更前→変更後」一覧を出してオーナー承認を得てからマージ。タスクごとに1ブランチ・1PR。
- この文書の「現状」は GSC / Cloudflare のデータ事実。コードの現状は書いていないので、着手前にリポジトリを読んで確認し、前提が違えば報告してから進める。
- 出典が確認できない情報（型番・自治体ルール・地区名・事例）は書かない。プレースホルダー＋オーナー確認リストにする。
- 順序：T0 → T1 → T2 → T3 → T4。各タスクの受け入れ条件を報告し、承認後に次へ。
- ビルド：`npm ci && npx eleventy` → `_site/`。監査除外は `_site/kaitori/` `_site/insta/` `_site/yt/`。

## 1. 背景データ（2026-08-20〜09-13）
- 表示 約30回/日で横ばい。クリックは実質1回/日（9/9・9/12 の10クリック中9件はトップ/about/area の1〜4位＝指名検索か自分の確認とみられる）。
- 表示の63%（456回）は3コラム、そこからのクリックは合計7回。/column/recall/ は 8/21以降184表示でサイト最大。「パナソニック 電動 自転車 リコール 2026」7〜8位、「型番」付きクエリが出現、「自転車 リコールとは」4位、「中古で購入した」3位。
- /column/battery-disposal/：「自転車 バッテリー 処分」20位前後、「処分方法」「捨て方」60位前後。一方「金沢市 リチウム電池 回収」8位、「白山市では？」5位。
- 「自転車買取 近く」：氷見1位・射水3位・小松4位・津幡4位・トップ7位（マップパック領域）。Yahoo 経由の訪問が 8/30〜9/8 に9回。
- /kanazawa/：順位6〜16位を往復、クリック0。/takaoka/：表示25・クリック5（CTR20%）。
- 計測：9/7・9/8 の GSC 欠損（weekly の gsc_days=5）。dailyFetch の _log が 9/11〜9/15 に無い。9/9 タブレット8訪問、9/12 LINE webview 10訪問、9/14 タブレット9訪問（自分作業の疑い）。

## T0. 計測の修正（meguri-report / GAS）
まず meguri-report のコードがローカル（clasp 等）にあるか確認。無ければ変更をパッチ形式で提示し、オーナーが GAS エディタに反映する。

### T0-1 欠損の補完
- fixDay('2026-09-07')、fixDay('2026-09-08') → buildWeekly → detectFlags を再実行。
- 受け入れ：weekly の 2026-09-07 行が gsc_days=7、gsc_daily に 09-07/09-08 の行がある。

### T0-2 トリガー停止の再発防止
- dailyFetch 冒頭で _log に start 行、例外は try/catch で error 行（現状は失敗が無記録）。
- weeklyReport 末尾で installTriggers を冪等に再実行（トリガー消失の自己修復）。
- buildWeekly 時に直近14日で gsc_daily の行が無い日（今日−3日以前に限る。GSC の遅延分は除く）を列挙して fixDay を自動実行。
- 9/10 の dailyFetch が 141s（通常40s前後）だった原因が分かれば報告。
- 受け入れ：手動でトリガー削除 → weeklyReport 実行 → トリガーが復活。行が無い日を作って buildWeekly → 自動で埋まる。

### T0-3 フラグの日割り正規化
- 表示・クリック・訪問の週比較は value×7/gsc_days（cf は cf_days）で正規化。gsc_days<7 の週はレポートに「n日分・暫定」を明記。
- 受け入れ：今週の battery-check / battery-disposal のフラグが正規化後の値で再計算される。

### T0-4 自分の訪問の検知（自動除外はしない）
- cf_dimensions で同日に deviceType=tablet または userAgentBrowser=ChromeMobileWebview の訪問が5以上 → フラグ「自分作業の可能性」。
- サイト側：`mcNoBeacon`（?notrack=1）が全ページ共通レイアウトで効いているか確認。LINE 内ブラウザはストレージが別なので、そこで一度 ?notrack=1 を開けば効くかも確認して報告。
- オーナー作業：タブレットと LINE 内ブラウザで https://meguri-cycle.com/?notrack=1 を一度開く。GSC 側は除外できないので、自サイト確認は URL 直打ちか GSC の URL検査で行う。

### T0-5 主要ページの定点観測
- 週次レポートに固定枠：/ 、/kanazawa/、/takaoka/、/column/recall/、/column/battery-check/、/column/battery-disposal/ の週次 表示・クリック・順位、および /review/ の Cloudflare 訪問数（T1 完了後）。
- 受け入れ：次回の水曜レポートに枠が出る。

## T1. GBP 口コミ導線（「近く」クエリ対策）
口コミ依頼そのものはオーナー作業。ここでは導線と文面を用意する。

### T1-1 /review/ リダイレクトページ
- /line/<slug>/ と同じ仕組みで /review/ を作成。GBP の口コミ投稿URL（オーナーから受領。未受領なら PLACEHOLDER）へ即時リダイレクト（JS + meta refresh + 手動リンク）。
- noindex。sitemap.xml は直接編集せず、既存の除外方法に従う。Cloudflare ビーコンは残す（/review/ の訪問数＝依頼への反応として計測）。
- 受け入れ：/review/ が GBP へ飛ぶ、noindex、ビルド成功。

### T1-2 口コミ依頼文（LINE 用、docs/review-request.md に保存）
- 取引完了後に送る短文。特典なし・家族投稿なし（景表法）。文面案：
  「本日はありがとうございました。もしよろしければ Google の口コミで感想をいただけると、次に自転車を手放す方の参考になります。率直な内容で大丈夫です。 https://meguri-cycle.com/review/ 」
- 返信ルールも併記：24時間以内、地域・サービスのキーワードを自然に、禁止語なし。
- オーナー承認後に確定。LINE ボットのプリセットには入れない（ボットは作り直し中）。

### T1-3 ローカル構造化データの確認
- トップ・市ページの LocalBusiness（相当）JSON-LD の有無を確認。あれば name / url / areaServed が GBP の表記・提供地域（石川県＋富山6市：高岡・富山・射水・氷見・砺波・小矢部）と一致するか確認。無ければ追加案を提示（住所・郵便番号・実名は入れない。areaServed に市町村名）。
- 受け入れ：差分一覧をオーナーに提示（実装は承認後）。

## T2. /kanazawa/ の作り込み（唯一の非テンプレ市ページ）
### 着手前の現状確認（報告してから進める）
- 市ページの生成方式（データファイル＋テンプレートか、個別ファイルか）。
- /kanazawa/ の本文文字数、他市ページとの本文類似率（サイト監査で使った BeautifulSoup/lxml の指標で before を取る）。
- 出張料の算出元（データファイル）と、金沢市がどの距離帯にまたがるか。

### 要件
- title / h1 / meta：「金沢市」「電動アシスト自転車」「買取」「出張」を自然に含める。禁止語なし。
- 金沢市専用セクションを本文の前半に追加（テンプレ本文は残してよい）：
  1. 金沢市で電動アシスト自転車のバッテリーを処分するには：金沢市の公式案内を一次情報として要約＋出典URL＋確認日。「処分できない」ではなく「膨張・破損で JBRC／メーカーが受け付けないもの」の表現。容量が残っていれば買取対象、と一行。/column/battery-disposal/#kanazawa へリンク。
  2. 出張料：データファイルの値を参照して「金沢市は○円〜○円（地区による）」。ハードコード禁止。
  3. 対応地区：金沢市の地区名を列挙（例：駅西・金石・森本・東山・小立野・額 など。オーナー確認）。
  4. 金沢市の買取事例：オーナーから受領した実績のみ（車種・年式・状態・診断結果・市内地区・価格の根拠）。無ければ節ごと作らない。「順次」「準備中」は使わない。
  5. 内部リンク：/column/recall/ と /column/battery-disposal/ から /kanazawa/ へ、/kanazawa/ から /dendo/ へ。
- 目安：本文 1,500〜2,500字。他市ページとの類似率を下げる。
- 受け入れ：変更前→後一覧、類似率 before/after、禁止語チェック通過、ビルド成功。

### オーナー作業
- GSC のページフィルタ /kanazawa/（3か月）で「金沢 自転車 買取」系クエリの有無を確認して共有。
- 金沢市の事例があれば提供。

## T3. /column/recall/ を型番検索型に
### 要件
- 一次情報のみ：消費者庁リコール情報サイト、Panasonic／Yamaha／Bridgestone の公式リコール告知。各行に出典URLと確認日。確認できない型番は書かない（取得できない場合は表の枠＋オーナー確認リスト）。
- 構成：
  1. 2026年の新規リコール（最上部。クエリ「リコール 2026」に対応）
  2. 型番の見つけ方（車体・バッテリーの型番位置。写真はオーナー提供、無ければ文章のみ）
  3. メーカー別の対象型番一覧（表：メーカー／型番／製造期間・対象範囲／対象部位／対応内容／メーカー窓口URL／出典／確認日）
  4. ページ内絞り込み：型番テキスト入力で表の行を絞る軽量 JS（外部ライブラリなし。JS 無効でも表は全部見える）
  5. FAQ：「リコールとは」「中古で購入した車体も対象？」「対象だった場合の流れ」
  6. 最終更新日と出典一覧
- 導線：控えめな CTA を1箇所。「リコール対象バッテリーはメーカーの無償対応をご利用ください。確認後に乗り換えをお考えの方は、バッテリー実容量診断つきで査定します」→ LINE。リコール対象バッテリーを買い取るとは書かない。
- title に「型番一覧」「2026」を含める。禁止語なし。
- 受け入れ：全行に出典あり、絞り込みが動く、変更前→後一覧、ビルド成功。

## T4. /column/battery-disposal/ を市町村別に
### 要件
- 冒頭の全国向け説明は短く（全国クエリは追わない）。以降は市町村別に見出しを切る：「金沢市で電動自転車のバッテリーを処分するには」形式。各節の id は市ページの slug と一致（#kanazawa 等。市ページが無い自治体は自治体名のローマ字）。目次を付ける。
- 第1弾：金沢市・白山市・野々市市・かほく市・津幡町・内灘町・小松市。第2弾：残りの石川県対応エリア。第3弾：富山6市（別ルールなので別節扱い）。
- 各節：自治体の公式案内の要約（収集対象外か／拠点回収か／協力店案内か）＋出典URL＋確認日、JBRC 協力店の探し方（JBRC 公式検索へのリンク。店名列挙はしない）、「容量が残っていれば買取対象」の一行＋該当市ページへのリンク。
- 表現：「処分できない」ではなく「膨張・破損で JBRC／メーカーが受け付けないもの」。膨張・液漏れ品の引き取り・処分は行わない旨を明記（現行方針）。
- 確認できない自治体は節を作らず、オーナー確認リストに載せる。
- 受け入れ：第1弾7自治体すべてに出典と確認日、目次のアンカーが動く、変更前→後一覧、ビルド成功。

### 監視
- 処分・診断の2記事は 9/9〜12 に表示がほぼ消えて 9/13 に復帰。T0-5 の定点観測で来週も低ければ、オーナーが GSC の URL検査でインデックス状態を確認。

## 報告フォーマット（各タスク共通）
- 着手前：現状確認の結果と、この文書の前提と違う点。
- 完了時：変更前→変更後一覧、ビルド結果、禁止語チェック結果、受け入れ条件の充足状況、残っているオーナー作業。

## 今回スコープ外（別途）
- LINE ボット新フロー、サイト監査の残り（/area/ の「順次」表現、富山市ページの「石川県内」ルール記述、og:image、metadata）。市ページのテンプレ自体を触る必要が出たら事前に相談。

---

## 進捗
- 2026-09-16 T0 完了（GAS 側 push・検証済み）。前提との差分：fixDay は未実装だったので新設／トリガー停止の原因は認可失効（installTriggers の自己修復では防げない）／meguri-report-gas は git 管理外のため PR 運用の対象外／9-7・9-8 の欠損は GSC の遅延中に認可が切れたため（バグではない）。
- 2026-09-16 T1 完了（PR #1 マージ、/review/ 本番公開を確認）。/review/ は GBP「クチコミを増やす」の URL（https://g.page/r/CdZATCD3-TNjEBM/review）へ即時リダイレクト。依頼文は docs/review-request.md。JSON-LD の差分：無し。GBP の提供地域は石川県＋富山7市（南砺市を含む）で登録済みと確認。要件定義 §T1-3 の「富山6市」は古い情報。
- T2〜T4 未着手（T2 は着手前の現状確認から）。
