# LINE 新フロー切替のロールバック控え（2026-09-16 作成）

要件定義（`Downloads/要件定義_LINEシステム.md`）N-3 / §12 段階0 の「事前に控えるもの」。
切替後に問題が出たとき、ここに書いてある状態に戻す。

## 1. 本番 GAS

| 項目 | 値 |
|---|---|
| scriptId | `1p914I_UIuyPkD7hDyuvum7ZPBwguzoGO4pxeilmPkPx1Df8-sxBkaMj_` |
| WebApp デプロイ ID | `AKfycbwjTqgrFyV8aFoXgykTvbmu0FBPcW0PoCWQ4aU7boELHmY0tFCMjhTIg0Wu8R6Yf79bDw` |
| 切替前の最終バージョン | **@47**（2026-09-14「手動対応中のタイル押し直しによる解除を廃止」） |
| 戻し方 | `npx clasp create-deployment -i <上のデプロイID> -V 47 -d "ロールバック"`（clasp の版により `clasp deploy -i … -V 47`） |
| リポジトリ側の対応コミット | `d4ac243`（`line-bot/code.gs`。実IDはプレースホルダ） |

転送シム（段階1で追加予定）を入れたあとの本番は @48 以降になる。ロールバック時は @47 に戻せば転送シムも消える。

**2026-09-16 切替実施**：本番 @49 ＝ repo `line-bot/code.gs`（実ID2行は本番値でフォールバック）＋ `v2-core/flows/quote/richmenu`、転送シム削除。form-sync / inquiry-sync / sheet-format / appsscript.json は本番のまま。メニューのデフォルト設定はオーナーが LINE で「メニュー切替」（戻すときは「メニュー戻す」＋ `-V 47` で再デプロイ）。

## 2. リッチメニュー（管理画面製・2026-09-14 時点）

Messaging API 製メニューはゼロ。以下はすべて manager.line.biz で作られたもの。

### メニューＡ（現在表示中）

| 項目 | 値 |
|---|---|
| 管理画面 ID | 19976879（URL: `/account/@136bpsyc/richmenu/19976879`） |
| 表示期間 | 2026/08/12 00:00 〜 2035/11/01 23:59 |
| テンプレート | 大サイズ・6分割（3列×2段） |
| 画像 | `line-richmenu-final.png`（リポジトリ直下） |
| メニューバーのテキスト | メニュー |
| メニューのデフォルト表示 | **表示しない**（2026-09-14 に「表示する」から変更。事故対応の応急処置） |

| 位置 | アクション | 送信テキスト |
|---|---|---|
| A 左上 | テキスト | 買取査定を申し込む |
| B 中上 | テキスト | 出張引取を申し込みます |
| C 右上 | テキスト | 対応エリア・出張費（末尾に半角スペース1つ） |
| D 左下 | テキスト | 写真は以上です |
| E 中下 | テキスト | よくある質問 |
| F 右下 | テキスト | 担当者に相談 |

スクリーンショット: `rollback_2026-09_menuA_detail.jpg`（変更前の詳細画面。デフォルト表示が「表示する」だった時点）

### メニューＢ（未使用）

| 項目 | 値 |
|---|---|
| 表示期間 | 2037/12/31 00:00 〜 2037/12/31 23:59（事実上無効） |
| アクション | テキスト: 写真を追加する / 台数を追加する / 進捗を確認 / 日程を変更したい / 担当者に相談 / キャンセル・やめる |

### ロールバック手順（メニュー）

1. API のデフォルトリッチメニューを解除する: `DELETE https://api.line.me/v2/bot/user/all/richmenu`
2. 対象ユーザーに個別リンクしたメニューがあれば外す: `DELETE https://api.line.me/v2/bot/user/{userId}/richmenu`
3. 管理画面のメニューＡが再び表示されることを自分の LINE で確認する
4. 管理画面のメニューＡ・Ｂは切替後2週間は削除しない（§12 段階6）

## 3. あいさつメッセージ（2026-09-16 時点・261文字）

管理画面 > トークルーム管理 > あいさつメッセージ（`/account/@136bpsyc/autoresponse/welcome`）
「はじめて友だち追加されたときにのみ送信」: **オフ**

先頭の2つは管理画面の差し込み変数（`{友だちの表示名}` / `{アカウント名}`）。

```
{友だちの表示名}さん、はじめまして！
{アカウント名}です🌈 友だち追加ありがとうございます🚲

メッセージが入力済みの場合は、そのまま送信してください。

不要になった自転車、写真を送るだけで無料査定します。

📷 カンタン2ステップ
1️⃣ 下のメニューから「買取を申し込む」をタップ
2️⃣ 案内にそって写真を送信

お伺いするのは金額が決まったあとだけ。
処分ご希望なら処分費0円（出張費のみ）で引取に伺います。

「売れるのかな？」という段階でも、1台からお気軽にどうぞ！
```

スクリーンショット: `rollback_2026-09_greeting.jpg`

切替時はタイル名に合わせて更新する（新メニューでも「買取を申し込む」のままなので、1️⃣ の文はそのまま使える見込み）。ロールバック時はこの全文に戻す。

## 4. その他

- 応答設定（チャット: オン、Webhook: オン）は変更しない
- 本番 Sheets「めぐり自転車_問い合わせ一元管理」（Drive id `19UxlX6FIsABenNRDTSHYbLEvehMuai72T0FIZBAZaAc`）に切替で追加するタブ: `sessions` / `quotes`、`users.cust_no` 列。ロールバック時は残しておいてよい（旧コードは読まない）

**2026-09-16 @50**：v2-flows / v2-quote のみ差し替え（買取フローにサビ質問を1問、確定金額の再査定条件に「広い範囲のサビ・固着」、受付通知に「見積を送る」ボタン、見積の相手ボタンに LINE 表示名）。開発 @28 で偽イベント11シナリオ確認済み。戻すときは `-V 49`。
