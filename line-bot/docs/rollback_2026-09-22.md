# 戻し方（2026-09-22 本番切替：写真フロー＋Push通数対策＋Discord通知）

## 本番ボット（GAS）
- 切替後の本番は **@63**（デプロイ ID `AKfycbwjTqgrFyV8aFoXgykTvbmu0FBPcW0PoCWQ4aU7boELHmY0tFCMjhTIg0Wu8R6Yf79bDw`）。
  同日の追加：@61 オーナーコマンド「手動解除」、@62 通知リンクを LINE チャット画面（chat.line.biz）に、@63 受付完了も Discord に統一（LINE Push はお客さま向けだけ）。
  @59・@60 は診断口の付け外し（選ばない）。
- 戻すときは、GAS エディタの「デプロイを管理」→ 鉛筆 → バージョンで **@55** を選んでデプロイ（URL は変わらない）。
  - @56 は転送シム入り（オーナーの LINE を開発版へ流す）、@57 は初期設定用の一時フック入りなので選ばない。
  - clasp なら `npx clasp create-deployment -i <上の ID> -V 55`（本番用の .clasp.json が要る。scriptId `1p914I_UIuyPkD7hDyuvum7ZPBwguzoGO4pxeilmPkPx1Df8-sxBkaMj_`）。
- 戻したあとも、スクリプトプロパティ `DISCORD_WEBHOOK_URL` と時間トリガー `v2PhotoStaleCheck`（1時間ごと）は残る。
  @55 のコードは使わないので害はないが、消したければプロパティは設定画面から削除、トリガーは「トリガー」画面から削除。
- 中央シートに `写真査定ログ` タブが増える（消さなくてよい）。

## サイト
- 公開のマージコミットは `d4e3a28`（撮影ガイド・バッテリー診断の新設、旧「3枚」表記の差し替え、サイトマップ2件）。
- 戻すときは `git revert -m 1 d4e3a28` → push（見本画像 `images/mihon/` は残る。ページから参照されないだけ）。

## 手作業修正リスト（Claude が触れないところ）
- LINE Official Account Manager の**あいさつメッセージ**に「写真3枚」「数枚」の表記があれば、「LINEで順番にご案内します」に直す。
- リッチメニュー（写真工程用）のタイル文言「写真は以上です」は、そのまま「次へ」として動く（変更不要）。
- Google ビジネスプロフィール・ジモティーの投稿に「写真3枚でOK」と書いていれば、同様に直す。
