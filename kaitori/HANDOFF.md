# 引き継ぎメモ — タブレット買取記入システム（kaitori/）

別PCの Claude Code で作業を継続するための引き継ぎ資料。

## このプロジェクトは何か
屋号 RAINBOW（めぐり自転車 / 個人事業主）の**出張買取現場向けタブレット記入システム**。
古物営業法の帳簿要件と電子帳簿保存法（電子取引データ）の保存要件を「仕組み」で満たすことが目的。
古物商許可番号：第511090015059号（石川県公安委員会）。譲受人：高多優典（連絡先はPDF非表示）。

- 公開想定URL：`meguri-cycle.com/kaitori/`（既存サイト `wilier300227-oss/meguri-cycle` のサブディレクトリ）
- 対象端末：Redmi Pad SE（Android / Chrome）＋ ディスク型タッチペン
- プリンター：EPSON EW-052A（Wi-Fi Direct 対応 / Bluetooth 非対応）

## 元になる指示書（リポジトリ同梱：`kaitori/docs/`）
- `docs/要件定義_タブレット記入システム.md`
- `docs/実装指示書_段階実装.md`（**フェーズ①→②→③の順に。次フェーズを先取りしない**）
- `docs/事務処理規程_電子取引データ.md`（電帳法 真実性のひな形。運用前に【　】を確定）

※ 別PCではまず上記3ファイルに目を通すこと。要約は本ファイルにもあるが、正は `docs/` の原文。

> **✅ 解決済み（2026-08-07）**：特定商取引法の「訪問購入」該当性を確認 → **適用外**と判断できた。
> これにより「出張での有償買取を行わない」制約は解除。書面交付義務・クーリングオフ前提の実装は不要。
> （経緯の詳細は末尾「特定商取引法（訪問購入）」を参照。当初の未解決事項は解消。）

## 全体の進め方ルール（厳守）
- 各フェーズは前フェーズの**実機確認OK後**に着手。次フェーズを先取り実装しない。
- 各フェーズ完了時に第8章「受け入れ基準」の該当項目を自己チェックし OK/NG 報告、次へ進んでよいか確認を求める。
- セキュリティ：GASトークンは PropertiesService、`.env` は非コミット。**本人確認書類の番号・写しは、運転免許証の「公安委員会名＋番号」以外は保存も表示もしない**（マイナ・保険証の番号欄は出さない）。
- ホスティングは GitHub Pages（`wilier300227-oss/meguri-cycle`、master ブランチ、独自ドメイン meguri-cycle.com）。GAS は `line-bot/*.gs` に集約する流儀。

---

## 現在の到達点：フェーズ1「実機確認済み・完了」（2026-08-06）

**スコープ**：通信・GAS・PWA なしで、端末単体で 入力→署名→確定ロック→PDF生成 まで動く。

### 作成ファイル（すべて `kaitori/` 配下・新規）
- `index.html` — 入力フォーム＋署名UI＋（画面外）A4印刷テンプレート2種
- `app.js` — 採番 / 年齢自動算出 / 本人確認欄の出し分け / 検証 / 確定ロック / PDF生成
- `style.css` — 画面UI＋A4テンプレートのスタイル
- `HANDOFF.md` — 本ファイル

### 実装済みの要点
- 法定5項目（取引年月日・品目/数量・特徴・相手方[氏名/住所/職業/生年月日=年齢]・本人確認方法）を必須化。未充足だと**確定ボタン不活性＋不足項目を一覧表示**。
- 生年月日→**年齢自動算出**。18歳未満で**保護者欄＋保護者署名を自動表示**。
- 本人確認区分プルダウン。**運転免許証選択時のみ**「公安委員会名・番号」欄を表示、他区分は非表示＋値クリア（PDF・後の台帳にも出さない）。
- 取引番号 自動採番 `RB-YYYYMMDD-NNN`（localStorage で日付別連番。確定時に確定）。
- 古物商許可番号を固定表示（編集不可）。
- `signature_pad` で譲渡人（＋未成年時は保護者）の手書き署名。HiDPI対応リサイズ。
- 「確定」で確定時刻（ISO8601）記録＋フォーム全体を readonly/disabled ロック、署名パッド無効化。
- PDFは **jsPDF標準フォントが日本語非対応のため、A4 HTMLテンプレートを `html2canvas` で画像化→jsPDF に配置**する方式。日本語・署名画像をそのまま埋め込み。
  - `8月6日 ○○様 買取.pdf`（譲渡証明書 兼 代金受領書・委任状 / A4・1枚。名前は取引区分に連動＝引取なら「引取」）
  - `8月6日 ○○様 保護者同意書.pdf`（**別ファイル** / A4・1枚 / 未成年時のみ）
  - 同じ内容を **PNG画像** でも保存可（LINE公式アカウントのチャットから送るため）
- CDN読込（signature_pad 4.1.7 / jsPDF 2.5.1 / html2canvas 1.4.1）。※オフライン化はフェーズ3。

### 実機確認・修正の記録（2026-08-06）
Redmi Pad SE 実機（指操作）＋ ヘッドレスChromeによる自動検証62項目で確認。**全項目PASS / JSエラー0**。
検証スクリプトはリポジトリ管理外（作業PCのスクラッチに `verify-phase1.mjs`）。

**自動検証で見つけて直した不具合**
- 署名は input/change を発火しないため、**最後に署名すると確定ボタンが不活性のまま**だった → `endStroke` で再検証。
- A4テンプレの実高さがA4を超え、**PDFの文字が縦に潰れていた** → 縦横比を保った縮小に変更＋テンプレの余白を圧縮。
- 買取金額がラベルに `*` があるのに未検証だった → 必須に追加（引取＝0円は `0` を入力）。
- favicon の404 → `../logo/favicon.png` を参照。

**実機フィードバックによるUI変更**
- 署名欄 180px → **260px**。キャンバス解像度の算出を実寸基準に直し、ペン先と線のズレも解消。
- 職業をプルダウン化（会社員（正社員）／契約社員・派遣社員／パート・アルバイト／自営業／会社役員／公務員／学生／主婦・主夫／年金受給者／無職／その他）。
- 生年月日を **年→月→日の3プルダウン**に変更（端末の日付ピッカーは指で年に到達しづらいため）。年は当年から100年分、日リストは年月に応じて絞る。値は hidden の `#pBirth` に `YYYY-MM-DD` で同期するので既存ロジックは不変。
- 郵便番号 → 住所の自動入力（zipcloud API）。**オンライン時のみ**。住所欄は常に手入力可。**フェーズ3のオフライン化では住所データのローカル同梱に差し替えること。**
- 公安委員会を石川／富山／福井＋「その他」のプルダウンに。既定は石川。「その他」は県外免許を記録するために残してある（消すと他県免許で本人確認方法が記録できなくなる）。
- 免許証番号は数字12桁のみ入力（「第」「号」は不要）。PDF出力時に `第000000000000号` に整形。
- メーカー／車種／色／続柄をプルダウン化（各「その他」で自由記述）。
- **特徴は必須のまま**（古物営業法の法定記載事項のため外せない）。代わりに メーカー／車種／色／フレーム番号／防犯登録番号 から**自動生成**して手入力ゼロで通るようにした。手で追記した場合は自動上書きせず、「自動入力し直す」ボタンで再生成。
- 譲受人の表記を `RAINBOW（めぐり自転車）` に。

**判断が必要だった点**
- 出力ファイル名を取引番号から**お客さま向けの名前**（`8月6日 ○○様 買取.pdf`）に変更した。署名直後にそのままLINEで送るため。**帳簿との照合は書面本文に印字した取引番号で行う。**
  → フェーズ2でDrive保存を作る場合は、要件定義2章の検索性要件を満たすため **Drive側のファイル名には取引番号を先頭に付けること。**
- 同じ月日・同じ氏名が別の年に出るとファイル名が重複する（手元のファイルは送信用の一時コピーという整理）。

### 受け入れ基準の自己チェック（第8章 該当4項目）
- [x] 法定5項目未入力時に署名確定・PDF生成ができない（強制クリックでもPDFは出ない）
- [x] 運転免許証以外の確認区分で番号入力欄が表示されない（PDF本文にも出ない）
- [x] 譲渡証明書と保護者同意書が別PDFとしてA4・1枚ずつ生成される（MediaBox 595.28×841.89 / 1ページを実ファイルで確認）
- [x] 署名確定後にフォームがロックされ確定時刻が記録される（33入力欄が readonly+disabled、署名パッド追記不可）

### 追加の実機フィードバック対応（2026-08-06 後半）
実機で触っての指摘を反映。自動検証は **73項目すべてPASS / JSエラー0**。

**画面の並び替え**
- 上＝**事業者記入欄**（取引情報 → 対象自転車 → 金額 → 本人確認）、下＝**お客さま記入欄**（相手方情報 → 誓約事項 → 保護者情報 → 譲渡人署名）。
  タブレットを渡す前に事業者が上を埋め、渡してからお客さまが下だけ触る運用。境目に色付きの区切り見出し（`.section-divider`）を入れてある。

**入力方式の変更**
- メーカーを**選択欄1つ・全体あいうえお順の29社**に（一度2段プルダウンにしたが、指摘を受け単一に戻した経緯あり）。`index.html` に静的 option として並べている。
- 色を **白／黒／シルバー／その他** の4択に絞った。
- 続柄を **父／母／祖父／祖母／兄弟／その他** に。
- 職業から「個人事業主」表記を外し「自営業」に。

**紙の書類（`Desktop/自転車事業書類/自転車譲渡証明書 兼 代金受領書・委任状.pdf`）との突き合わせ**
紙にあってシステムに無い項目を洗い出し、**リスクの大きい5つを追加**した。
- **譲渡人の電話番号**（必須）— 記入間違いに後から気づいた時の連絡手段
- **防犯登録の名義人**（必須／本人・本人以外・登録なし不明）。本人以外を選ぶと名義人氏名・続柄・**名義人の同意チェック**が必須になる。盗品リスクの切り分けの要
- **会社・法人名義でないことの誓約**（必須チェック）。誓約文も「盗品・遺失物・不法投棄品ではなく」に拡張
- **付属品**（鍵〈本数〉／バッテリー／充電器／取扱説明書）。電動アシストのバッテリー有無は後日トラブルになりやすい
- **保護者の住所**（「未成年者と同じ住所を入れる」ボタンあり）と**保護者の本人確認方法**（写し提出済み／現地で原本確認）

3行増えてA4を超えたため、テンプレの行間・セル余白を再調整して1123px（A4ちょうど）に収め直した。

### 既知の注意点
- 検証用にHTTP（`http://<PCのLAN IP>:8765/kaitori/`）で開くと、ChromeがPDF/画像のダウンロードを「安全にダウンロードできません」と警告する。**これは非HTTPS起点のダウンロードに対するChromeの仕様で、本番の `https://meguri-cycle.com/kaitori/` では出ない。**
- 端末の共有メニューからLINEに送ると、タブレットにログインしている**個人アカウント**から送信される。めぐり自転車として送るには **LINE公式アカウントアプリ**でログインし、1:1チャットからPDFまたはPNGを送ること。

---

## 次フェーズ（未着手・先取り禁止）

> **2026-08-06 時点の方針**：PDFは公式アカウントアプリから手動で送れているため、
> 「Drive保存＋LINE公式アカウントからの自動送信」は**不要**と判断された。
> ただし下記フェーズ2の**台帳・保存の部分は法令要件（古物台帳の保存・電帳法の検索性/バックアップ）に直結する**ため、
> 着手可否は別途判断すること（現状はPDFが端末内にしか残らず、要件定義4章「端末単独保存にしない」を満たしていない）。

### フェーズ②：GAS台帳・Drive連携 —— ✅「デプロイ・実機確認済み・完了」（2026-08-07）
特商法が**適用外**と確認できたため（末尾の未解決事項は解消）、台帳・保存に着手して実装。
LINE自動送信は方針どおり作らず、**台帳追記＋Drive保存**のみ（法令要件対応）。
GASを新規プロジェクトとしてデプロイ済み、実機で 署名→台帳追記→DriveへのPDF保存→台帳リンク まで確認済み。
（GAS URL・トークンは `kaitori/config.js`＝非コミットに保持。スクリプトプロパティにシートID/フォルダID/トークンを登録済み。）

**追加・変更ファイル**
- `line-bot/kaitori-ledger.gs`（新規）— GAS Web App。`doPost` で取引データ＋PDF(base64) を受け、Drive にPDF保存 → 台帳に1行追記 → Driveリンク記録。**LINEボット(code.gs)とは別GASプロジェクトとして新規デプロイ**（1プロジェクトに `doPost` は1つのみ）。ファイル冒頭に初回セットアップ手順あり。
- `kaitori/config.sample.js`（新規・コミット可）／ `kaitori/config.js`（**非コミット**・`.gitignore` 済み）— GAS URL・トークン。
- `kaitori/{app.js,index.html,style.css}`（変更）— 確定後に PDF を base64 化して自動送信。失敗時は localStorage キュー(`rb_ledger_queue`)に退避し、**起動時・`online` 復帰時に自動再送**。出力パネルに送信ステータス欄＋「再送」ボタン。`sheetToPdfBase64()` 追加。
- `.gitignore`（変更）— `kaitori/config.js` を追加。

**要点**
- 台帳カラムは要件定義3.5＋実機フィードバックの増分（電話番号・防犯登録名義人・付属品・保護者住所・保護者本人確認）も保持。
- **免許証以外は確認番号を空**：フロント（`collectRecord`）とGAS（`handleAppend_`）の二重防御。「その他」選択は `pick()` で実値に解決して送信。免許証番号は `第…号` に整形。
- 買取金額は台帳に**数値**で保存（検索・集計可）。取引年月日・相手方氏名も独立カラム＝検索要件充足。
- Driveファイル名は**取引番号を先頭**に（`RB-…_譲渡証明書.pdf`）。※端末保存用のお客さま向けPDF/PNG名（`8月6日 ○○様 買取.pdf`）とは別で、両立している。
- 訂正は GAS 側に `action:'correct'`（元行の訂正ログ追記＋新規行）を用意。フロントの訂正UIは未実装（運用は新番号で新規登録）。

**セットアップ（実機確認前に必要）**：`kaitori-ledger.gs` 冒頭手順に沿って新規GASプロジェクト作成 → スクリプトプロパティ `KAITORI_TOKEN`/`KAITORI_SHEET_ID`/`KAITORI_FOLDER_ID` 登録 → `initLedgerHeader` 実行 → ウェブアプリでデプロイ → URLとトークンを `config.js` に設定。

**自己チェック（第8章 該当3項目・コードロジック上）**
- [x] 台帳が取引日・金額・相手方氏名で検索できる（独立カラム・金額は数値）
- [x] 免許証以外の確認区分で確認番号が台帳に保存されない（フロント＋GAS二重防御）
- [x] PDFがDriveに取引番号名で保存され、台帳リンクで開ける

**⚠ セキュリティ（要判断）**：GitHub Pages は静的配信のため `config.js` のトークンは公開ソースから閲覧され得る。`.gitignore` でGit履歴からは守られるが、公開配信するならトークンの定期ローテーション＋kaitori非公開運用を推奨。

**別PCでの落とし穴（今回発生）**：ローカルが古いまま作業すると、GitHub上の最新（プルダウン等）を取りこぼす。**作業前に必ず `git fetch && git status` で origin と同期を確認すること。**

### フェーズ③：PWAオフライン対応・印刷仕上げ

**③-A：PWA化＋オフライン化 —— ✅ 実装・動作確認済み（2026-08-07）**
- `manifest.json`＋`sw.js`（Service Worker）で PWA化。アプリシェル（HTML/JS/CSS/ライブラリ/アイコン/北陸住所データ）をキャッシュし、**オフラインで 入力→署名→PDF/画像生成 まで完結**。
- CDNライブラリを `kaitori/vendor/` にローカル同梱（vendoring）：signature_pad 4.1.7 / jsPDF 2.5.1 / html2canvas 1.4.1。index.html はvendor参照に変更。
- 台帳・Drive送信（GAS）はオンライン時のみ。オフライン中は localStorage キューに退避し、**`online` 復帰時に自動再送**（フェーズ②の flushQueue を活用）。SWは別オリジン（GAS/zipcloud）を素通しさせオフライン時は自然に失敗→フォールバック。
- **郵便番号→住所のオフライン対応**：日本郵便データから**北陸3県（石川・富山・福井）**を抽出し `kaitori/data/zip_hokuriku.json`（7,595件・約316KB／gzip約61KB）として同梱。lookupは「ローカル辞書優先（オフラインでも即時）→ 県外はオンライン時 zipcloud → オフライン＋県外は手入力」。全国同梱は数MBになるため北陸3県に限定（出張範囲をカバー）。
- アイコン：`kaitori/icons/`（192/512/512-maskable）を `logo/profile-1080.png` から生成。
- 実機確認：シークレットウィンドウ（http://localhost 配信）で、オフライン時にページ表示・北陸の住所検索が動作することを確認。
- ⚠ **SW更新の注意**：`sw.js` を変更したら `CACHE` の版数を上げる（現在 `kaitori-v5`）。開発中にコード変更が反映されない時は、DevTools→Application→Service Workers→Unregister＋Clear site data、またはシークレットで確認。**タブレット側は旧版キャッシュを持つため、修正反映にはオンラインで一度開き直す（PWAは一度閉じて再起動）必要がある。**

**③-A-3：タブレットPDF崩れ修正＋確定の確認ダイアログ —— ✅ 実装・本番反映（2026-08-07）**
実機フィードバック対応。
- **PDF/PNG崩れ（タブレットで枠切れ・拡大）**：原因は html2canvas がモバイルで `position: fixed` 要素を可視ビューポート基準でズレて拡大キャプチャする不具合。`.a4-stage` を `fixed→absolute` にし、`renderSheet()` に `width/height/windowWidth/windowHeight/scrollX,Y:0/x,y:0` を実寸で明示して端末非依存に固定。PC(dpr1.5)・本番v4で canvas 1588×2246・A4比0.7070を確認。
- **お客さまの誤タップ対策**：「署名を確定する」押下時に確認ダイアログ（`#confirmModal`）を表示。検証NG時は出さず、OKで `doConfirm()` が実処理（ロック＋台帳自動送信）。native `confirm()` ではなく自前モーダル。`onConfirm()`＝検証＋ダイアログ表示、`doConfirm()`＝実確定、に分離。
- SWキャッシュ v3→v4。ライブ検証：確認ダイアログの表示/キャンセル/外側タップ、PDF寸法を本番URLで確認済み。**タブレット実機での最終確認（崩れ解消・ダイアログ動作）は要実施**。

**③-A-4：台帳連携設定の隠蔽＋完了ボタン —— ✅ 実装・本番反映（2026-08-07）**
- **設定カードを既定で非表示**（`#ledgerSettings` に `hidden`）。**タイトル(`#appTitle`)を約1.5秒長押し**で表示する隠し操作（`setupTitleLongPress()`、pointer系イベント・移動/短タップでキャンセル）。お客さまが誤って開き「消去」でトークンを飛ばすのを防ぐ。初回設定後はほぼ使わない前提。
- **「完了（次のお客さまへ）」ボタン**を出力パネルに追加（`#btnFinish`）。確認ダイアログ(`#finishModal`)ののち `location.reload()` で初期化。PWAでオフラインでも動作。**台帳設定・未送信キューは localStorage 保持のため消えない**。
- SWキャッシュ v4→v5。ライブ検証：非表示既定/長押し表示/短タップ非表示、完了ダイアログの表示・キャンセル・外側タップを本番URLで確認済み。

**③-A-5：ホーム画面アイコン拡大＋アプリ名変更 —— ✅ 実装・本番反映（2026-08-07）**
- PWAアイコンを `logo/profile-1080.png` から**被写体(自転車・ピン)ズーム構図**で再生成（`kaitori/icons/` を上書き）。any(192/512)は全面、maskableはクリーム地(#FAFAF8)に90%配置で安全余白を確保。生成はPillow（`zoom1.30`／crop=(70,100,531,561)／中心=(W/2, H*0.55)）。
- `manifest.json` の `name`/`short_name` を **「めぐり自転車フォーム」** に変更。SWキャッシュ v5→v6。
- ⚠ **タブレット反映**：アイコン・名前の変更はAndroidがインストール時にキャッシュするため、**ホーム画面の既存アイコンを一度削除→オンラインで開き直し→再度「ホーム画面に追加」で入れ直す**必要がある（リロードだけでは旧アイコン/旧名のまま残ることがある）。

**③-A-6：予測変換/自動入力による個人情報の漏えい対策 —— ✅ 実装（2026-08-08。タブレット実機確認は未了）**
共有タブレット（Redmi Pad SE）で、氏名・住所・電話などの入力時に**前のお客さまの個人情報が予測変換／自動入力候補として表示される**という指摘への対応。
- `app.js`：`hardenInputsForPrivacy()` を追加し `init()` で実行。`#kaitoriForm` 内の `text/tel/number/textarea` 全欄に `autocomplete="off" / autocorrect="off" / autocapitalize="off" / spellcheck="false"` を付与（今後フィールドを増やしても自動適用）。フォーム要素自体の `autocomplete="off"` は既存。
- `sw.js`：`CACHE` を `v6→v7`。
- **効果と限界**：これで抑止できるのは**Chromeの自動入力**とブラウザ/一部キーボードの予測表示まで。**Androidキーボード（Gboard等）の「学習辞書」そのものはWebから制御不可**。共有端末では端末側でも対策が必要（下記）。
- **端末側で必須の設定（Redmi Pad SE / Gboard 想定）**：Gboard設定 →「テキストの修正」で**入力候補・次の入力候補・個人辞書ベースの候補をオフ**、→「単語リストと学習」で**学習した単語・データを削除（ゲスト/共有運用ならさらに学習オフ）**。Xiaomi独自キーボード使用時は同等の「予測入力／学習をオフ・学習データ消去」を実施。
- ⚠ **反映手順**：PWA＝SWキャッシュのため、タブレットは一度**オンラインで開き直す**（PWAを閉じて再起動、必要なら Chrome DevTools/Application で更新）と `v7` に切り替わる。
- ⚠ **未了**：タブレット実機で「前の人の情報が出ないこと」の最終確認。

**③-A-7：オフライン時のPDF/PNG崩れ（署名が巨大化）修正 —— ✅ 実装（2026-08-08。タブレット実機確認は未了）**
実機で「オフラインの時だけ PDF/PNG が崩れる（③-A-3と同じ枠切れ・拡大）」との指摘。切り分けの結果、**アプリ＋オンライン＝正常／オフラインだけ崩れる**と判明。崩れた実物を確認すると**本文は正常で、署名画像だけが原寸(高解像度)のまま巨大化して枠からはみ出し**ていた。
- **原因**：`buildCertSheet()` が署名 `<img src="data:image/png;…">`（HiDPI・高解像度）を挿入した**直後**に `renderSheet()`→`html2canvas` を実行。画像が**デコード＆レイアウト（CSSの `max-height:66px` 適用）される前**に取り込まれると、署名が原寸で描画されはみ出す。**オフラインは生成が速く未デコードで走りやすいため再現**（data URLなのでネットワーク自体は無関係。オンラインは些細な待ちでレイアウトが確定し"たまたま"正常だった）。
- **修正（`app.js`）**：`waitForImages(sheetEl)` を追加し、`renderSheet()` の冒頭で**全画像の `decode()` 完了＋次フレーム**まで待ってから html2canvas を実行。ネットワーク状態に依存せず常にレイアウト確定後に取り込む。
- `sw.js`：③-A-6 と同じ `v7`（未デプロイの同一版に同梱）。
- ⚠ **未了**：タブレット実機（オフライン）で署名が正しいサイズで出ることの最終確認。③-A-3の修正が「PC・オンライン」検証止まりで**オフライン実機が未検証**だったのが今回の再発の遠因。

**③-A-8：保存パネル（PDF/PNG）を署名確定前に表示しない —— ✅ 実装・タブレット実機（オフライン）確認済み（2026-08-09）**
実機で「署名確定の前に PDF/PNG 保存の画面が出ており、お客さまが誤って押してしまう可能性がある」との指摘。
- **原因**：出力パネル（`#outputPanel`＝「PDF出力」＝保存画面）を表示する経路は本来 `doConfirm()`（確認ダイアログ「確定する」押下後）だけのはずが、`init()` 末尾に **「未送信キューがあれば起動時に `outputPanel` を `show`」** する経路（`app.js`）が残っていた。台帳送信が失敗してキューが残った端末では、**完了→リロード後の起動時に、まだ署名していない新規フォームで保存パネルが開く**。PDF/PNGボタン自体は `isLocked` ガードで空押し無効だが、パネルが見える＝誤タップ懸念どおりの状態だった。
- **修正（`app.js`）**：起動時のキュー再送で **パネルを開かず裏で `flushQueue()` のみ** 実行するよう変更（`if (readQueue().length) flushQueue();`）。保存パネルの表示は `doConfirm()` の1箇所に一本化。未送信分は `online` 復帰時・次回確定時（`enqueueAndSync`→`flushQueue`）に自動再送＆ステータス表示される。
- `sw.js`：`CACHE` を `v7→v8`。
- **確認**：タブレット実機（オフライン）で、署名確定→確認ダイアログOK後にのみ保存パネルが出ることを確認済み。

**③-A-9：オフライン時のPDF/PNG「拡大・枠切れ」の根治 —— ✅ 実装・タブレット実機（LAN配信で完全オフライン）確認済み（2026-08-09）**
③-A-3／③-A-7 と同型（枠なし・全体拡大・枠切れ）の崩れが**また実機オフラインで再発**との指摘。過去2回は毎回 **PC・オンライン（このバグが再現しない条件）で修正・検証→本番→実機オフラインで再発**を繰り返していた。今回は原因を2系統から断ち、**pushする前にLAN配信で実機オフライン確認**してから反映した（再発ループの遮断）。
- **原因（幾何）**：シートは画面外 `.a4-stage { left:-9999px }` に置いてキャプチャする。モバイルの html2canvas はこの画面外オフセットと実ビューポート幅(<794px)とのズレで、取り込み原点が狂い**シート全体が拡大され枠が切れる**。
- **原因（timing）**：オフラインは生成が速く、`max-height` 等のレイアウト反映前に html2canvas が走る競合に負けやすい（オンラインは通信待ちで“たまたま”確定後になる）。
- **修正（`app.js`）**：
  - `renderSheet()` に **`onclone`** を追加。キャプチャ用の複製ドキュメント側だけでステージを原点(0,0)に戻して取り込む（実DOMは非変更＝チラつき・オンライン動作に影響なし）。画面外オフセットのズレを根本から除去。
  - `waitForImages()` を強化：画像デコード待ちに加え **`document.fonts.ready` ＋ 強制リフロー(`getBoundingClientRect`) ＋ 2フレーム待ち**でレイアウト確定を保証。
- `sw.js`：`CACHE` を `v8→v9`。
- **確認**：Redmi実機で LAN配信（`http://<PC>:8765/kaitori/`）→確定後に機内モード→PDF/PNG生成で崩れ無し、さらに完全オフラインでも崩れ無しをユーザー確認済み。
- **教訓（重要）**：kaitori のPDF/PNG描画を触ったら **PCでOKでも「直った」と報告せず、必ず実機オフラインで確認してから push** すること（PC・オンラインでは再現しない）。

**③-A-10：生年月日「年」プルダウンの既定位置を成人あたりに —— ✅ 実装・LAN実機確認済み（2026-08-09）**
実機フィードバック。「年」プルダウンの既定が空プレースホルダ（先頭＝近年）のため、ネイティブ選択が
近年（子ども側）の位置で開き、成人の生年まで大きくスクロールが必要だった。
- **修正（`app.js`）**：`initBirthSelects()` で「年」の既定選択を **当年 − `BIRTH_DEFAULT_AGE`(=40) 年**（例:2026→1986）に設定。プルダウンは選択中項目の位置で開くため、開いた瞬間に成人あたりの年が表示される。既定年齢は定数 `BIRTH_DEFAULT_AGE` で一元管理。
- **安全性**：年だけ既定選択でも月・日が未選択の間は `#pBirth` は空＝年齢計算・確定ボタンは無効のまま（中途半端な有効化なし）。「年」プレースホルダ（空）も選択肢に残す。※操作者が既定年のまま月日だけ選ぶと誤生年になり得る点は運用注意（お客さま署名前に本人が目視できる書面のため二重チェックはかかる）。
- `sw.js`：`CACHE` を `v9→v10`。
- **確認**：LAN配信でタブレット実機にて「年」が既定1986表示・タップ時に成人年付近で開くことを確認済み。

### ④：複数台対応フォーム（別ページ `kaitori/multi.html`）—— 実装・本番公開（2026-08-11）
`Desktop/買取契約書_印刷用_複数台用.pdf` を基準に、**1取引で最大5台**をまとめて記入できる複数台版フォームを
**別ページで新規作成**（既存の単台フォーム index.html/app.js/style.css は無変更）。
- **新規ファイル**：`kaitori/multi.html`／`app-multi.js`（自己完結。採番/生年月日/zip/署名/`renderSheet`/`waitForImages` は app.js から流用）／`style-multi.css`（明細表・名義人・複数ページ）／`manifest-multi.json`（start_url=multi.html＝複数台を別PWAアイコンで追加可）。
- **内容（PDF準拠）**：明細（最大5台・追加/削除・メーカー/車種/色/車体番号/防犯登録番号/買取金額）／合計買取金額（自動合計＋手修正可）／本人確認（免許証番号あり）／相手方情報／誓約2／**防犯登録の名義人①〜④＋対象車体No.＋各自の同意署名**／未成年時の保護者同意書（別PDF・2ページ目）／委任状。**受付担当者欄は無し**（任意項目の記入漏れ対策で削除）。
- **複数ページPDF**：`paginateCertSheets()` がブロック単位でA4に詰め、超えたら次ページへ（明細行・署名がページ境界で切れない）。`generateCertPdf()` は各ページを `renderSheet()`→jsPDF に `addPage()` で連結。PNGは複数ページ時に連番保存。
- **オフライン（PWA）**：`sw.js` に複数台3ファイル＋`manifest-multi.json` を同梱、`CACHE` を **v10→v11**。`app-multi.js` でSW登録。
- **台帳連携（1車両=1行）**：確定時に**車両ごとに1行**を送信（取引番号は枝番付き `RB-YYYYMMDD-NNN-01`…、共通の相手方情報を各行に複製）。**既存GAS(kaitori-ledger.gs)は無改修**で `append` を車両数ぶん呼ぶ。PDFは枝番01の行にだけ添付（Drive重複回避）。設定・キューは単台と同じ localStorage を共有。名義人列は名義人の「対象車体No.」一致でベストエフォート判定。
- ⚠ **未検証（要実機）**：**複数ページPDFの実機オフライン描画（崩れ）は未確認**。過去③-A-3/7/9 と同型リスクがあるため、機内モードでPDF生成して崩れが無いか実機確認すること。SW版上げのためタブレットは一度オンラインで開き直す（PWA再起動）と v11 に切替。台帳は4台入力で枝番-01〜-04の4行が入り再送で重複しないことを確認すること。

**③-A-2：台帳連携の設定を「端末保存」に変更 —— ✅ 実装済み（2026-08-07。ブラウザ実機の最終確認は未了）**
③-A末尾の「⚠未確定（デプロイ設計）：config.js にトークンを載せるとGitHub Pages公開ソースから読める」問題への対応。
**トークンを公開ソースに置かず、各端末で一度だけ入力してlocalStorageに保存する方式**にした。
- `config.js` は**空の既定値だけ**の公開安全版にして**コミット対象に変更**（`.gitignore` から除外を解除、`config.sample.js` は役割を終えたので削除）。
- 画面下部に `<details>` の「⚙ 台帳連携の設定（この端末に保存）」カードを追加（`index.html`）。GAS URL・トークンを入力→「この端末に保存」で `localStorage['rb_ledger_config']` に保存、「消去」で削除。
- `app.js`：`loadConfig()` が **localStorage優先→無ければ config.js の既定**で `CFG` を解決。`saveLedgerConfig()`/`clearLedgerConfig()`/`showCfgStatus()` を追加し `init()` で配線。保存成功時に未送信キューがあれば `flushQueue()` を呼ぶ。未設定時の送信ステータス文言も設定カードへ誘導する内容に更新。
- `style.css`：`.settings-card` のスタイル追加。`sw.js`：`CACHE` を `v2→v3`。`config.js` は `OPTIONAL`（404許容）のまま。
- **効果**：GitHub Pages に公開してもソースにトークンが載らない。タブレット側は初回に設定カードで入力すれば以後は端末内に保持。→ ③-A末尾の「未確定（デプロイ設計）」は解消。
- ⚠ **残**：ブラウザでの保存/消去の動作確認は環境要因（拡張が権限を持つローカルサーバのポート＝`127.0.0.1:8765` が旧セッションの別ルートサーバに占有されていた）で最終確認未了。curlでは設定カードを含むHTMLの配信を確認済み。実機タブレットでの初回設定→送信の確認が本来の最終確認。

**③-B：印刷仕上げ —— ⏸ 保留（プリンター選定中）**
- Wi-Fi Direct 経由で EW-052A へ印刷する導線（端末標準印刷 or EPSON Smart Panel 連携）。**使用プリンター未確定のため保留**。決まり次第着手。

### 最後（フェーズ③後）
- セットアップ手順書（GASデプロイ / 端末インストール / 印刷設定 / 台帳初期ヘッダー）。
- スコープ外（防犯登録の電子データ受理可否の事前確認、事務処理規程の備付け）は「運用前の残タスク」として手順書に明記。

---

## 特定商取引法（訪問購入）— ✅ 解決済み（2026-08-07：適用外と確認）

> **結論（2026-08-07）**：確認の結果、本件は訪問購入の規制の**適用外**と判断できた。以下は判断に至るまでの検討記録として残す。
> ※ 判断の根拠（どの取引形態・確認先の回答内容で適用外になったか）は運用記録として別途明記しておくことを推奨。

### 何が問題だったか
**出張買取は特定商取引法の「訪問購入」に該当する可能性が高い。** 訪問購入の適用除外品目は政令で6つだけ
（①自動車〈二輪を除く〉②家庭用電気機械器具〈携行が容易なものを除く〉③家具 ④書籍 ⑤有価証券
⑥レコードプレーヤー用レコード・磁気テープ・蓄音機用レコード）で、**自転車はどれにも入らない**。
電動アシスト自転車も軽車両であって家庭用電気機械器具ではないため同様。

**誤解しやすい点**：お客さまから「買い取りに来て」と依頼された出張でも規制は外れない。
依頼があれば外れるのは「不招請勧誘の禁止」だけで、**書面交付義務とクーリング・オフは適用される。**

### 該当する場合に必要になること
- 申込書面・契約書面の交付（法定記載事項＋赤枠・赤字などの様式要件）
- **クーリング・オフ（書面受領日から8日間）**
- クーリング・オフ期間中は物品の引渡しを拒めることの告知
- 期間中に第三者へ引き渡す場合の売主への通知・引渡先への通知
- 書面には**購入業者の氏名・住所・電話番号**の記載が必須
- 書面は原則その場で紙で交付（電子交付は令和3年改正で可能になったが事前承諾など要件が厳しい）
  → **その場でEW-052Aから印刷する導線が必要になる＝フェーズ3の印刷を前倒しする必要がある**
- 8日間は転売できない前提の在庫管理

### 該当しない取引
- **持込買取**（営業所等での取引は訪問購入ではない）
- **無料引取（0円・無償譲渡）**（「購入」ではない）
- 相手が事業者、相手が海外、国・地方公共団体 など

### 事業者側の事情（重要な制約）
事業主の意向として **電話対応が苦手・プライバシー確保のため住所と電話番号は公開しない方針**。
これについて整理した結論：
- **法定書面はその取引の相手1人に渡す紙であって、サイトへの掲載義務ではない。** サイト非公開の方針は維持できる。
- 電話番号は「記載」の義務であって「常時応答」の義務ではない。050のIP電話を取得し留守電＋LINE誘導で足りる。
  クーリング・オフの通知は書面（はがき等）でも有効。
- **住所の完全秘匿は元々成立していない**：古物営業法11条により、行商時は許可証の携帯義務があり、
  取引の相手方から請求されれば提示しなければならない。許可証には氏名・住所が記載されている。
- 書面に書く住所は**古物商許可で届け出た営業所の所在地と揃える**こと。バーチャルオフィスは営業所要件と衝突しやすい。

### 確認先・確認内容
**2026-08-07（金）に電話予定。** 8/11（火）は山の日で祝日のため、8/12 までの営業日は 8/7 と 8/10 のみ。
- 石川県 県民生活課／消費生活支援センター、または 消費者庁 取引対策課
- 聞くこと：「古物商として一般消費者の自宅を訪問し、自転車を**有償で**買い取る場合、訪問購入に該当するか」

### 直近の予定
**2026-08-12（水）に出張査定が1件ある。** 確認結果が出るまでの方針は
**「査定と無料引取までにとどめ、その場で有償買取契約を結ばない」**。
これなら書面義務が発生せず、電話番号もその場印刷も不要。有償買取は後日持込または準備完了後に回す。

### 該当が確定した場合の実装（未着手・確認後に着手）
- 取引情報に**引渡方法（持込／出張）**を追加
- **出張 かつ 買取（有償）のときだけ**譲渡証明書を2ページ構成にし、2ページ目に赤枠のクーリング・オフ告知を出す
  （`sheetToPdf` を複数シート→複数ページ対応に拡張する想定。現在は1シート＝1ページ固定）
- クーリング・オフ期限（交付日＋8日）を自動計算して明記
- 引渡し拒絶権を告知した記録のチェック欄（事業者側）と、説明を受けた確認欄（お客さま側）
- 訪問購入モードのときだけ譲受人の住所・電話番号をPDFに表示（通常は非表示のまま）
- EW-052A へのその場印刷導線（フェーズ3から前倒し）

---

## 残タスク一覧（2026-08-06 時点）

### 最優先
1. **特商法の訪問購入該当性の確認**（8/7 電話）→ 結果を本ファイルに追記すること
2. 該当する場合、上記「該当が確定した場合の実装」一式
3. **古物商許可証で「行商する」になっているか確認**。営業所の住所も確認（特商法書面の住所と揃える）

### 書類の内容（紙の書類にあってシステムに無い、リスク中の項目・未着手）
「あった方がよいもの」として洗い出し済み。追加するかは未決定。
- 氏名のフリガナ
- 引渡方法（持込／出張）と出張費・受領済チェック ※特商法対応でも必要になる
- 本人確認の「原本を目視で確認」チェックと確認者名
- 防犯登録の登録都道府県、「登録なし・不明」チェック
- 振込の場合の入金期日
- 保護者同意書の同意事項2項目（取引内容の説明を受け了承／法定代理人である旨）

### 運用開始前に必ず片付けること
- **保存年数の統一**：紙の書類は「3年間保管」、`docs/事務処理規程_電子取引データ.md` は税務に合わせ「7年」。どちらかに揃える
- **事務処理規程の【　】を確定して備付け**（電帳法 真実性の要件）
- 防犯登録の電子データ受理可否を管轄の防犯協会・警察署に事前確認
- 050番号などの事業用連絡先の手配（特商法該当時のみ必要）

### 保存・台帳（フェーズ2 相当・着手可否は未決定）
現状**PDFが端末のダウンロードフォルダにしか残らない**ため、要件定義4章「端末単独保存にしない」と
電帳法の検索性・バックアップ要件を満たしていない。LINE自動送信は不要と判断されたが、
台帳＋Drive保存の部分は法令要件に直結するため別途判断が必要。

あわせて **訂正機能**の設計も未着手（記入間違いに後から気づいた場合の扱い）。整理した結論：
- 誤記の訂正（フレーム番号・住所の番地など内容の実体が変わらないもの）は**再署名不要**、訂正記録を残せば足りる
- 金額・対象物・氏名など相手方の権利義務に関わる訂正は**相手方の同意が必要**（対面での再署名でなくLINEでの同意記録でも可）
- いずれの場合も**元のPDFを消して差し替えるのは不可**。元を残し、訂正版を新規発行し、訂正記録（申請日・取引番号・訂正日・内容・理由・担当者）を残す
- 様式は `docs/事務処理規程_電子取引データ.md` 第5条と末尾の「取引情報訂正・削除申請書」がそのまま該当
- 実装案：台帳から元取引を呼び出して訂正版を作成。取引番号は `RB-20260806-001-訂正1` のように元番号を引き継ぐ。
  台帳に訂正ログ行を追加し元の行は残す。金額・車体の特定・譲渡人氏名を変更しようとした場合だけ
  「相手方の同意が必要」と警告し再署名または同意記録の入力を求める

### 公開 —— ✅ 本番公開済み（2026-08-07）
- **`feature/kaitori-phase1` を `master` にマージ＆push済み。GitHub Pages で `https://meguri-cycle.com/kaitori/` として本番公開（ビルドbuilt・200確認）。**
  ライブ検証：設定カード表示OK／公開config.jsは空（秘密なし）／`data/zip_hokuriku.json`配信OK／`sw.js`は`kaitori-v3`。
- 公開＝URLを知れば誰でも開ける状態（`noindex` 済）。事業主の判断で公開に踏み切った。
- **タブレット初期設定（残・ユーザー作業）**：Redmi Pad SE の Chrome で上記URLを開く → 画面下部「⚙ 台帳連携の設定」に **GAS ウェブアプリURL** と **トークン（GASの`KAITORI_TOKEN`と同値）** を入力→「この端末に保存」 → メニュー「アプリをインストール（ホーム画面に追加）」でPWA化 → 機内モードで 入力→署名→PDF/画像 とオフライン住所検索が動くことを確認。
- ✅ ブラウザ確認（2026-08-07）：本番URL `https://meguri-cycle.com/kaitori/` で、設定カードの保存→「✅有効」表示→localStorage保存→消去で初期化、を確認。Service Worker は activated、北陸辞書7,595件で石川/福井の郵便番号が解決、東京は該当なし（＝北陸限定が有効）。**本来の最終確認はタブレット実機**（初回設定→機内モードでの一連動作）。

### 検証環境の再現方法（別PC向け）
- リポジトリ直下で `python -m http.server 8765` を起動し、タブレットから `http://<PCのLAN IP>:8765/kaitori/` を開く。
  Windowsファイアウォールで `python.exe` のインバウンド許可が必要。
- 自動検証は Puppeteer + インストール済みChrome。スクリプトは**リポジトリ管理外**（作業PCのスクラッチに
  `verify-phase1.mjs`／`shot.mjs`）。必要なら `npm i puppeteer-core` で作り直す。Chrome拡張（claude-in-chrome）は未接続だった。

---

## リポジトリ状態（2026-08-07 更新）
- ブランチ：`master` に **公開済み**（GitHub Pages の公開ブランチは `master`）。`feature/kaitori-phase1` もpush済み。
- `kaitori/` は新規追加。既存ファイルは未変更。
- 実データ（顧客情報）はまだ扱っていない。localStorage は採番の連番のみ使用。
- 郵便番号検索のみ外部通信あり（zipcloud）。それ以外は端末内で完結。

### コミット履歴（`feature/kaitori-phase1`）
- `8530a1c` フェーズ1の初版（別PCで作成）
- `2a7a8bc` 元資料を `docs/` に同梱
- `8286b44` フェーズ1の不具合修正と実機フィードバック反映（署名後の再検証、PDF縦潰れ、買取金額必須、各プルダウン化、
  生年月日3段、郵便番号検索、ファイル名を顧客向けに変更、PNG保存追加、譲受人を RAINBOW（めぐり自転車）に）
- （本コミット）画面の並び替え、メーカーを単一プルダウン29社に、色4択、続柄6択、リスク大の5項目追加、本ファイル更新
