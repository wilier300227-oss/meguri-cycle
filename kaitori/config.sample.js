/* =========================================================
   買取システム 接続設定 —— サンプル（このファイルはコミットしてよい）
   使い方：このファイルを同じフォルダに config.js という名前で複製し、
           下の2つを実際の値に書き換える。config.js は .gitignore 済み（非コミット）。
   ========================================================= */
window.KAITORI_CONFIG = {
  // GAS Web App（kaitori-ledger.gs）のデプロイURL（.../exec で終わる）
  gasUrl: '',
  // GAS のスクリプトプロパティ KAITORI_TOKEN と一致させる秘密トークン
  token: '',
};
